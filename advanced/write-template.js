webpackJsonp([13,25],{703:function(s,e){s.exports={content:'<p>Template is the most significant concept. the processing is follows.</p><ol><li><p>Get user\'s answers by <code>prompts</code>.</p></li><li><p>Read <code>root</code> files, filter the files by <code>ignore</code>.</p></li><li><p>Transform file content using <a href="/advanced/write-loader">Loader</a></p><ul><li>Rule one(top priority): matching the text from the file\'s first line.</li></ul><pre><code class="hljs language-text"data-query={} data-lang=text>// @loader ${LOADER_NAME}?${QUERY}\n/* @loader ${LOADER_NAME}?${QUERY} */\n# @loader ${LOADER_NAME}?${QUERY}\n&lt;!-- @loader ${LOADER_NAME}?${QUERY} --></code></pre><ul><li>Rule two(normal priority): matching loader from the <code>test</code> field of <code>mappers</code>.</li></ul></li><li><p>Adjust files struction by <code>move</code> and <code>copy</code>.</p></li><li><p>Trigger <code>usefulHook</code> then <code>post</code> hooks</p></li></ol><p><strong>Note：The order of fields in <code>package.json</code> is that <code>edam:main</code> is superior to <code>main</code>. (>=3.1)</strong></p><h2 id=config><a href=#config aria-hidden=true><span class="icon icon-link"></span></a>Config</h2><p>Allows exporting config object directly, or callable.</p><p><code>module.exports = { /*config*/ }</code> <code>module.exports = edam => ({ /*config*/ })</code></p><h3 id=process><a href=#process aria-hidden=true><span class="icon icon-link"></span></a>process</h3><p><code>edam >= 2.0.1</code> Returns the object which excludes <code>prompts</code>.</p><ul><li>type: <code>function</code></li></ul><p>Eg：</p><pre><code class="hljs language-javascript"data-query={} data-lang=javascript>{\n  <span class=hljs-function><span class=hljs-title>process</span>(<span class=hljs-params>answer</span>)</span> {\n    <span class=hljs-keyword>return</span> {\n      <span class=hljs-attr>ignore</span>: [],\n      <span class=hljs-attr>root</span>: <span class=hljs-string>\'anc\'</span>\n    }\n  }\n}</code></pre><h3 id=prompts><a href=#prompts aria-hidden=true><span class="icon icon-link"></span></a>prompts</h3><p>User\'s interaction definition.</p><ul><li><p>type: <code>[]</code></p><p>references to <a href=https://github.com/SBoudrias/Inquirer.js/>inquirer.js</a></p></li></ul><h3 id=root><a href=#root aria-hidden=true><span class="icon icon-link"></span></a>root</h3><p>Template files\' root.</p><ul><li>type: <code>string</code></li><li>default: <code>./template</code></li></ul><h3 id=ignore><a href=#ignore aria-hidden=true><span class="icon icon-link"></span></a>ignore</h3><p>Files\' filter</p><ul><li><p>type: <code>string[]</code></p><p>Or <code>answers => []</code></p></li></ul><h3 id=variables><a href=#variables aria-hidden=true><span class="icon icon-link"></span></a>variables</h3><ul><li><p>type <code>{}</code></p></li><li><p>example</p><pre><code class="hljs language-javascript"data-query={} data-lang=javascript>{\n  <span class=hljs-attr>date</span>: <span class=hljs-keyword>new</span> <span class=hljs-built_in>Date</span>(),\n  <span class=hljs-attr>val</span>: <span class=hljs-string>\'abc\'</span>\n}\n<span class=hljs-comment>// or</span>\n(answers) => ({})</code></pre></li></ul><h3 id=hooks><a href=#hooks aria-hidden=true><span class="icon icon-link"></span></a>hooks</h3><pre><code class="hljs language-javascript"data-query={} data-lang=javascript>hooks: {\n  <span class=hljs-string>\'post\'</span>: [<span class=hljs-string>\'touch Readme.md\'</span>, <span class=hljs-function><span class=hljs-keyword>function</span> (<span class=hljs-params></span>) </span>{ ... }]\n}</code></pre><h3 id=loaders-和-mappers><a href=#loaders-%E5%92%8C-mappers aria-hidden=true><span class="icon icon-link"></span></a>loaders 和 mappers</h3><p>Check out <a href="/advanced/write-loader">Write a loader</a></p><h3 id=move><a href=#move aria-hidden=true><span class="icon icon-link"></span></a>move</h3><pre><code class="hljs language-json5"data-query={} data-lang=json5>{\n  <span class=hljs-string>"package.json.js"</span>: <span class=hljs-string>"package.json"</span>,\n  <span class=hljs-string>"test/**"</span>: <span class=hljs-string>"tests/"</span>,\n  <span class=hljs-regexp>//</span> special placeholder: [path] <span class=hljs-regexp>/ [name] /</span> [ext] / [base]\n  <span class=hljs-regexp>//</span> eg.  root/abc.js ->\n  <span class=hljs-regexp>//</span>   path: `root/`\n  <span class=hljs-regexp>//</span>   name: `abc`\n  <span class=hljs-regexp>//</span>   ext: `.js`\n  <span class=hljs-regexp>//</span>   base: `abc.js`\n\n  <span class=hljs-regexp>//</span> root<span class=hljs-regexp>/a.js.hbs => root/</span>a.js\n  <span class=hljs-string>"**/*.hbs"</span>: <span class=hljs-string>"[path][name]"</span>\n}</code></pre><p>or <code>(answers) => ({})</code></p><h3 id=copy><a href=#copy aria-hidden=true><span class="icon icon-link"></span></a>copy</h3><p>like <code>move</code></p><h3 id=usefulhook><a href=#usefulhook aria-hidden=true><span class="icon icon-link"></span></a>usefulHook</h3><p>Some useful shortcuts of <code>post</code> hook.</p><ul><li><p>gitInit</p><p><code>git init</code></p><ul><li>type: <code>boolean</code></li><li>default: <code>false</code></li></ul></li><li><p>installDependencies</p><p>install dependencies of <code>package.json</code></p><ul><li>type: <code>boolean</code></li><li>default: <code>false</code></li></ul></li><li><p>installDevDependencies</p><p>install devDependencies of <code>package.json</code></p><ul><li>type: <code>boolean</code></li><li>default: <code>false</code></li></ul></li></ul><p>或者 <code>(answers) => ({})</code></p>',extra:{"_image-loader_":[]}}}});
//# sourceMappingURL=write-template.js.map